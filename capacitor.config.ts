import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.finishingpro.app',
  appName: 'FinishingPro',
  webDir: 'dist/public'
};

export default config;
